import React from 'react';
import {ScrollView, View, Text} from 'react-native';

const UpdateSale = () => {
  return (
    <ScrollView style={{flex: 1}}>
      <View>
        <Text>UpdateSale</Text>
      </View>
    </ScrollView>
  );
};

export default UpdateSale;
